[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "iyou_chart_kernel"
version = "0.1.0"
description = "High-performance family tree chart generation engine"
requires-python = ">=3.8"

[tool.maturin]
module-name = "iyou_chart_kernel"
